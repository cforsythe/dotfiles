# .gitconfig.local - Machine-specific git configuration
# This file is not tracked in git. Copy to ~/.gitconfig.local and fill in.
#
# [user] is REQUIRED — the main gitconfig has no email/name set.

[user]
	name = cforsythe
	email = cforsythe.cs@gmail.com

# Machine-specific safe directories
# [safe]
#     directory = /path/to/repo
